// alert('create');
var cancel_vote_link =  '<%= link_to(cancel_vote_path(@episode, @tag), method: :delete, remote: true) do %><input type="checkbox" id="<%= @tag.id %>" class="p_g_tag_checkbox" checked><% end %>'
$('#episode_<%= @episode.id %>_tag_<%= @tag.id %>_container').html(cancel_vote_link);