// alert('sfsddf');
var vote_link =  '<%= link_to(votes_path(@episode, @tag), method: :post, remote: true) do%>\
                    <input type="checkbox" id="<%= @episode.id.to_i * 10 + @tag.id.to_i %>" class="p_g_tag_checkbox">\
                  <% end %>'


$('#episode_<%= @episode.id %>_tag_<%= @tag.id %>_container').html(vote_link);

$('#episode_<%= @episode.id %>_tag_<%= @tag.id %>_container').removeClass('p_g_checked');