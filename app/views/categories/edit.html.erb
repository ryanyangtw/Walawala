<div class="col-md-6 col-md-offset-3 well">
	<h2>修改節目分類</h2>
	<%= simple_form_for @category do |f| %>

		<%=render :partial=>"form", :locals=>{:f=>f} %>

		<%= f.submit "修改節目分類", :class=>"btn btn-primary"%>
	<%end%>